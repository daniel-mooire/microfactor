display_name: Factor Research
short_description: Build microfactor research factors
default_prompt: Extract factor candidates from this research input, standardize them as microfactor FactorSpec records, and generate reviewable factor code.
