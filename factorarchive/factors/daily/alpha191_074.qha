version = 1
name = "alpha191_074"
frequency = "1d"
formula = "rank(corr(sum((low * 0.35) + (vwap * 0.65), 20), sum(mean(volume, 40), 20), 7)) + rank(corr(rank(vwap), rank(volume), 6))"
inputs = ["low", "volume", "vwap"]
min_window = 65
direction = "unspecified"
category = "price_volume"
adjust = "none"
implementation = "microfactor.factors.alpha191_document::alpha191_074"
implementation_status = "exact"
status = "candidate"
source_type = "document"
source_group = "Alpha191-9f867946"
reference = "9f867946daeec1a1a84d0e3e7edff93a96485a94effe240d85f0d8c3f3a3f13e"
adaptation = "The formula is evaluated in the source-supported fq=None mode so OHLC and VWAP remain in the same unadjusted price units. The signal uses information through the T-day close and is evaluated against the standard T+1 open return. Normalization audit: Only case and redundant parentheses were normalized."
tags = ["document", "alpha191", "daily"]
parameters_json = "{\"alpha191_number\":74,\"normalization\":\"Only case and redundant parentheses were normalized.\",\"normalized_expression\":\"rank(corr(sum((low * 0.35) + (vwap * 0.65), 20), sum(mean(volume, 40), 20), 7)) + rank(corr(rank(vwap), rank(volume), 6))\",\"recursive_stability_window\":null,\"source_expression\":\"RANK(CORR(SUM(LOW*0.35+VWAP*0.65,20),SUM(MEAN(VOLUME,40),20),7))+RANK(CORR(RANK(VWAP),RANK(VOLUME),6))\",\"source_fq\":null}"
